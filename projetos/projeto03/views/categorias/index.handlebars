<h4 class="mb-2">Categorias: </h4>
<hr>
{{#each categorias}}
    <div class="card mt-2">
        <div class="card-body">
            <a href="/categorias/{{slug}}">{{nome}}</a>
        </div>
    </div>
{{else}}
    <h5>Nenhuma categoria encontrada.</h5>
{{/each}}