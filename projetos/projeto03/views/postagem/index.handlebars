<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        {{postagem.conteudo}}
        <hr>
        <small>Data de criação: {{postagem.data}}</small>
    </div>
</div>